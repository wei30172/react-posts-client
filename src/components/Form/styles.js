const FieldStyle = {
  width: '97%',
  margin: '10px 0'
}

const ButtonStyle = {
  marginTop: 1
}

const PaperStyle = (theme) => ({
  margin: theme.spacing(1),
  padding: theme.spacing(2)
})

const FormStyle = {
  display: 'flex',
  flexWrap: 'wrap',
  justifyContent: 'center',
}

export { FieldStyle, ButtonStyle, PaperStyle, FormStyle }